FROM mattermost/mattermost-team-edition:9.9

# This is a minimal Dockerfile that just uses the official Mattermost image
# The docker-compose.yml handles the actual configuration and database setup
# You can extend this Dockerfile with additional customizations if needed
